# LiveKit Agent Framework v1.x with plugins
# Using extras syntax for better plugin installation
livekit-agents[silero,deepgram,openai]>=1.0.0,<2.0.0

# Core LiveKit SDK
livekit>=0.18.0

# HTTP client for API calls
httpx>=0.27.0

# Environment management
python-dotenv>=1.0.0
